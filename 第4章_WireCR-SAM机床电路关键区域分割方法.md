# 第4章 基于多尺度卷积适配与类感知提示的 SAM 机床电路关键区域分割方法（WireCR-SAM）

本章围绕机床电路图像中的关键区域分割任务展开研究。依托第3章构建的机床电路关键区域分割数据集，本文系统阐述 WireCR-SAM 模型的设计思路、关键模块、训练策略及实验分析。本章仅讨论工业机床电路图像中的关键区域分割问题，不涉及第5章中的字符增强、OCR 识别与系统实现内容。为保证方法描述与实际实现保持一致，以下模型结构、训练流程和指标定义均以当前方法实现为依据。

## 4.1 问题描述与总体思路

机床电路图像中的关键区域分割属于典型的复杂工业视觉任务，其难点主要体现在目标形态、背景环境和下游应用需求三个方面。在线束区域中，目标通常呈细长连续分布，局部宽度较小且弯折明显，这使像素级提取高度依赖模型对局部结构的刻画能力。接口孔洞则属于小尺度目标，数量有限但定位要求严格，背景纹理稍有干扰便可能导致漏检。与此同时，控制柜内部普遍存在端子、金属边框、铭牌、阴影、反光和密集纹理等复杂背景，容易诱发自然场景预训练模型的边界粘连、区域外扩和局部缺失等问题。考虑到分割结果将直接服务于后续的字符裁剪与检测分析，本任务不仅要求较高的区域定位精度，还要求模型保持细线结构的连通性，并提高孔洞目标的召回能力。

基于上述场景特征，本文将机床电路关键区域提取建模为三类语义分割问题，类别分别为背景 `background`、线束 `wire` 和接口孔洞 `hole`。给定输入图像 $X \in \mathbb{R}^{H \times W \times 3}$，模型需要输出同尺度的语义预测结果 $\hat{Y}$，其映射关系可表示为：

$$
\hat{Y} = F_{\Theta}(X), \quad \hat{Y} \in \mathbb{R}^{H \times W \times 3}
$$

式中，$F_{\Theta}(\cdot)$ 表示本章提出的 WireCR-SAM 分割模型，$\Theta$ 为模型参数集合。

针对该任务，本文没有采用对 SAM 进行全量微调的策略，而是在尽量保留其通用分割能力的基础上，通过轻量化特征适配与自动提示构造实现工业场景迁移。具体而言，模型利用 SAM 图像编码器提取高维视觉嵌入，再通过 WireCR 适配器对编码特征进行多尺度增强；在此基础上，模型依据增强后的特征自动生成类别相关的提示向量，并调用共享的掩膜解码器逐类完成前景掩膜预测；最终结合隐式背景构造策略重建三类语义 logits，得到关键区域分割结果。

与原始交互式 SAM 相比，本文方法针对批量离线工业检测流程做出了三方面改造。

- 通过多尺度卷积适配器注入工业任务先验，在不显著增加训练参数的前提下增强模型对细线和小孔洞结构的表达能力。
- 通过类感知可学习提示替代人工点提示和框提示，使模型能够自动完成 `wire` 与 `hole` 两类目标的分割解码。
- 通过 BCE、Dice、Boundary 和 clDice 组成的联合损失函数，同时约束整体区域精度、边界一致性、细线连通性与孔洞召回能力。

总体而言，本文方法的核心流程可概括为“图像编码 - 特征适配 - 提示生成 - 逐类解码 - 语义重建 - 联合优化”。这一设计在保留 SAM 通用预训练优势的同时，摆脱了人工 prompt 对工业批量推理流程的限制，因此更适合机床电路关键区域的自动分割任务。图4-1给出了原始 SAM 与 WireCR-SAM 的总体流程对比，其中左侧为“图像编码器 + 人工 prompt + 掩膜解码器”的原始流程，右侧为“图像编码器 + WireCR adapter + 类感知 prompt + 逐类解码 + 语义 logits 重建”的本文流程。

## 4.2 原始 SAM 模型分析与适配需求

### 4.2.1 原始 SAM 模型组成

Segment Anything Model（SAM）是一种典型的提示驱动型通用分割模型，其主体由图像编码器、提示编码器和掩膜解码器三部分构成。对于输入图像 $X$，图像编码器负责提取全局视觉表征，并将图像映射为高维嵌入：

$$
E = \mathrm{Enc}_{img}(X)
$$

式中，$E$ 表示图像特征嵌入。在当前实现中，SAM 主干支持 `vit_b`、`vit_l` 和 `vit_h` 三种模型规模，图像编码输出通道数固定为 256。

在原始 SAM 中，用户通常需要提供点提示、框提示或已有掩膜提示。提示编码器负责将这些外部交互信息转换为稀疏提示嵌入与稠密提示嵌入，随后掩膜解码器在图像嵌入和提示嵌入的共同作用下完成目标区域预测，其基本过程可抽象为：

$$
M = \mathrm{Dec}_{mask}(E, P_{s}, P_{d})
$$

式中，$P_{s}$ 和 $P_{d}$ 分别表示稀疏提示和稠密提示，$M$ 表示输出掩膜预测结果。

从模型设计角度看，SAM 具有两项突出优势。一方面，依托大规模自然图像与掩膜数据完成预训练后，模型具备较强的开放域分割能力；另一方面，提示驱动机制使其能够针对不同目标灵活生成区域分割结果，因而在交互式标注和任意目标提取任务中表现出较好的适用性。这些优势使 SAM 成为工业场景迁移的重要基础。

### 4.2.2 原始 SAM 在工业语义分割中的局限

尽管 SAM 在自然图像场景中表现出较强泛化能力，但将其直接应用于机床电路关键区域分割时仍存在若干明显局限。预训练域与目标域之间的分布差异是首要问题。原始 SAM 主要在自然图像上学习显著性先验，而机床电路图像中普遍存在高密度布线、金属纹理、局部反光、标签边框和复杂遮挡等工业特征。这类场景与自然图像在纹理分布、目标形态和背景结构上存在明显差异，因而会削弱编码器对工业细粒度区域的判别能力。

从交互方式看，原始 SAM 的优势建立在人工 prompt 的参与之上，但工业检测流程更强调批量处理和自动化推理。若仍依赖人工为每张图像、每个目标逐一提供点提示或框提示，不仅会显著增加使用成本，也难以满足系统部署对一致性和重复性的要求。换言之，提示机制若不能从人工交互转化为特征驱动的自动生成形式，SAM 的交互式优势就很难转化为工业场景下的工程优势。

从目标属性看，机床电路线束属于典型的细长结构目标，接口孔洞则是面积较小但定位要求严格的局部目标。前者要求模型在复杂背景中保持拓扑连通和边界完整，后者要求模型在较小感受区域内维持稳定的检出能力。原始 SAM 更擅长处理轮廓清晰、显著性较强的区域目标，对于复杂背景中的细线和小尺度结构，常出现断裂、粘连或漏检等现象。

从任务形式看，原始 SAM 面向的是“任意目标区域提取”，而不是“固定语义类别解码”。本任务不仅要区分前景与背景，还要进一步区分 `wire` 与 `hole` 两类具有明确语义含义的工业目标，因此模型需要建立编码特征与类别语义之间的稳定映射关系。仅依靠交互式提示很难完成这一过程，这也是本文引入类感知提示生成机制的重要动因。

### 4.2.3 适配需求分析

综合上述分析，机床电路关键区域分割对 SAM 的迁移并不是简单的任务微调，而是需要围绕特征表征、提示机制和优化目标同时展开改造。模型改造应尽量限制在轻量模块内，以便在保留 SAM 预训练知识的同时增强其对工业局部结构和多尺度纹理的感知能力；提示向量应由图像特征自动生成，从而摆脱人工交互输入对批量推理流程的限制；训练目标则需要同时刻画区域重叠度、边界准确性、细线连通性和小目标召回性能，以保证模型输出能够满足下游任务对结构完整性和定位精度的双重要求。

基于这一认识，本文没有引入独立的后处理分类网络，也没有额外叠加复杂的精修分支，而是直接围绕“编码特征适配”和“自动提示构造”对 SAM 进行参数高效改造。这样做的原因在于，过多的附加分支虽然可能提升局部性能，却容易削弱方法的结构清晰性和工程可控性；相反，若能够在原始编码器和解码器框架内完成工业场景迁移，就更有利于保留 SAM 的通用表示能力，同时保持整个分割链路的紧凑性与可复用性。最终形成的 WireCR-SAM 既保留了原始模型的基本骨架，又在工业语义分割所需的关键环节上实现了针对性增强。

## 4.3 WireCR-SAM 总体框架

### 4.3.1 模型整体流程

WireCR-SAM 的总体框架建立在原始 SAM 图像编码器与掩膜解码器之上，其新增模块主要包括 WireCR 多尺度适配器、类感知提示嵌入和提示生成器。与原始交互式分割流程不同，本文方法不再依赖外部人工提示，而是通过编码特征自适应生成类别提示，以完成机床电路图像中的多类语义分割。

在前向传播过程中，输入图像经由 SAM 预处理后送入图像编码器，获得基础视觉嵌入。随后，基础嵌入进入 WireCR 多尺度适配器，在不破坏原始特征整体语义结构的前提下完成针对工业场景的局部增强。增强特征经过全局平均池化与提示生成器后，被转换为各前景类别对应的 prompt 偏移向量，并与可学习的类别基础嵌入叠加，形成 `wire` 与 `hole` 两类稀疏提示表示。

完成提示构造后，模型调用共享的 SAM 掩膜解码器，对各类别提示逐类解码，得到逐类前景 logits。由于解码器结构在不同类别间保持共享，类别差异主要由稀疏提示承担，因此模型能够在不重写解码头的情况下完成多类语义预测。最终，前景 logits 被进一步重组为完整的三类语义 logits，背景类别则通过隐式背景构造策略恢复。这样形成的输出既延续了 SAM 的掩膜建模方式，又满足了语义分割任务对固定类别输出的要求。

上述过程可抽象表示为：

$$
E^{\prime} = \mathcal{A}_{\phi}(E)
$$

式中，$E$ 为原始图像编码特征，$\mathcal{A}_{\phi}(\cdot)$ 表示 WireCR 适配器，$E^{\prime}$ 为增强后的特征表示。

需要指出的是，本文并未为背景类单独设计一条提示解码路径，而是仅对前景类别执行逐类解码，再通过隐式背景构造策略恢复完整的三类语义分布。这样既降低了解码复杂度，也使有限的可学习提示容量集中服务于 `wire` 与 `hole` 两类关键目标。

### 4.3.2 隐式背景构造与语义输出

设前景类别数为 $C-1$，在本任务中有 $C=3$，即前景类别为线束和接口孔洞。令 $Z_k$ 表示第 $k$ 个前景类别的掩膜 logits，则最终语义 logits 的重建方式为：

$$
Z_k = \mathrm{Dec}_{mask}(E^{\prime}, P_k, P_d), \quad
S = \mathrm{Concat}\big(-\max_{k}(Z_k), Z_1, \ldots, Z_{C-1}\big)
$$

式中，$P_k$ 表示第 $k$ 类的稀疏提示向量，$P_d$ 表示从 SAM prompt encoder 中取得的无掩膜稠密提示，$S$ 表示最终的语义 logits。对 $S$ 执行 `argmax` 操作后，即可得到背景、线束和接口孔洞三类的像素级语义预测结果。

该设计与当前实现保持一致：模型对每个前景类别分别调用同一套 SAM 掩膜解码器，逐类收集低分辨率掩膜和质量预测，再统一上采样并拼接为语义分割结果。相较于重新训练一个新的多类解码头，这种方式能够更充分地保留 SAM 掩膜解码器已有的区域建模能力。

### 4.3.3 模型特征与工程优势

从工程实现角度看，WireCR-SAM 的框架设计兼顾了参数效率、结构统一性和实验可扩展性。图像编码器与提示编码器默认冻结，仅训练 WireCR 适配器、类别嵌入和提示生成器，因此模型能够在保留预训练知识的同时降低训练成本。对于工业场景而言，这种参数高效的迁移方式不仅更符合有限数据条件下的训练需求，也有助于减少全量微调带来的过拟合风险。

与此同时，主任务 `wire_hole` 与辅助任务 `coco` 共享同一训练入口和评估接口，方法中的结构变量、损失变量和样本比例变量也都能够在统一框架下完成切换。这一实现方式使模型天然适合开展结构消融、损失消融和少样本实验，实验设计与工程实现之间不存在割裂。换言之，WireCR-SAM 并非仅仅给出一个静态模型结构，而是形成了一套可直接支撑系统比较与后续扩展的实验框架。

图4-3给出了类感知 prompt 生成与逐类掩膜解码流程，完整展示了“增强特征 - 全局平均池化 - MLP - 类别提示偏移 - 类别嵌入叠加 - 逐类解码 - logits 拼接 - argmax 输出”的处理链路。

## 4.4 WireCR 多尺度适配器设计

WireCR 适配器是本文方法的核心模块，其设计思想来源于 CRNet 中的双路径多尺度卷积结构，但针对 SAM 编码特征的二维视觉表示进行了重构。与原始 CRNet 面向 CSI 任务的设计不同，本文适配器直接作用于 SAM 图像编码器输出的二维嵌入特征，重点提升模型对工业图像中局部结构复杂、尺度变化明显和背景干扰较强场景的适应能力。

### 4.4.1 输入投影与多尺度编码

SAM 图像编码器输出通道数固定为 256，而不同规模适配器的内部特征维度并不相同。为提升结构灵活性，本文采用 $1 \times 1$ 卷积将原始嵌入投影到适配器隐藏空间，随后利用双路径多尺度编码器提取局部结构特征。该过程可表示为：

$$
H_{ms} = \mathrm{Fuse}\big(f_{long}(W_{in}E), f_{short}(W_{in}E)\big)
$$

式中，$W_{in}$ 表示输入投影层，$f_{long}(\cdot)$ 表示长路径卷积分支，$f_{short}(\cdot)$ 表示短路径卷积分支，$\mathrm{Fuse}(\cdot)$ 表示通道拼接与 $1 \times 1$ 卷积融合操作。

其中，长路径采用 `3×3 -> 1×9 -> 9×1` 的各向异性卷积组合，更有利于建模线束等长条状目标的方向性与上下文关联；短路径则通过相对轻量的局部卷积捕获细粒度纹理变化。双路径结构能够在保留局部细节的同时扩大有效感受野，因此更适合处理“细长线束、小孔洞和复杂边框”共存的工业图像结构。

从实现方式看，`small`、`medium` 和 `large` 三种适配器规模主要通过隐藏通道数、编码中间通道数和 `CRBlock` 通道数加以控制。其中，`small` 更适合显存受限场景下的快速验证，`medium` 适合作为论文主实验配置，`large` 则用于分析更强表达能力与计算开销之间的平衡关系。

### 4.4.2 压缩-扩张瓶颈与上下文门控

在完成多尺度编码后，本文进一步引入压缩-扩张瓶颈结构，以降低适配器参数量并增强特征重组能力。设压缩-扩张模块记为 $B(\cdot)$，上下文门控记为 $G(\cdot)$，则该阶段可表示为：

$$
H_{g} = B(H_{ms}) \odot \big(1 + G(B(H_{ms}))\big)
$$

式中，$\odot$ 表示逐元素乘法。具体而言，压缩层将隐藏通道压缩为较低维表示，压缩比例由 `compression_ratio` 控制；扩张层再将其恢复到原隐藏维度。为增强全局上下文调制能力，本文在瓶颈之后引入基于全局平均池化与 `1×1` 卷积的上下文门控，使适配器不仅依赖局部卷积响应，还能够依据整幅图像的语义统计特征对通道响应进行动态调节。

这一设计的价值主要体现在两个层面。一方面，通过设置 `compression_ratio=4/8/16/32/64`，可以系统分析参数规模与性能之间的关系，并为后续结构消融提供清晰的变量基础。另一方面，上下文门控能够在复杂背景中抑制无关纹理干扰，使适配器不仅关注局部卷积响应，还能结合整幅图像的全局统计特征调节通道权重，从而提高对关键区域结构的响应稳定性。对于机床电路图像中常见的标签、边框和金属反光等干扰区域，这种全局调制机制尤为重要。

### 4.4.3 卷积精炼与残差注入

为进一步提升适配器输出质量，本文在瓶颈与门控之后加入 `5×5` 卷积和两个连续 `CRBlock` 模块，对多尺度特征进行进一步精炼，并最终通过输出投影层回映射到与 SAM 编码特征相同的通道空间。最终适配器输出可表示为：

$$
E^{\prime} = E + \lambda \, W_{out}\big(R(H_{g})\big)
$$

式中，$R(\cdot)$ 表示由 `5×5` 卷积和双 `CRBlock` 组成的精炼模块，$W_{out}$ 表示输出投影层，$\lambda$ 为可学习输出缩放参数。

残差注入的意义在于，模型无需完全重建原始编码特征，而是重点学习与工业任务相关的增量信息。这样既保留了 SAM 预训练特征中的通用视觉知识，又允许适配器围绕线束边界、孔洞轮廓和局部结构连续性进行定向增强。

除完整版本外，代码中还保留了 `adapter_simple` 简化变体。该变体去除了显式压缩-扩张瓶颈，可作为结构消融实验中的轻量对照模型，用于比较瓶颈和门控机制对性能的具体影响。图4-2给出了 WireCR adapter 的内部结构，涵盖“输入投影 - 双路径多尺度编码 - 压缩-扩张瓶颈 - 上下文门控 - `5×5` 卷积与双 `CRBlock` 精炼 - 输出投影 - 残差相加”等关键环节。

## 4.5 类感知提示生成与联合优化策略

### 4.5.1 类感知提示构造

原始 SAM 依赖外部交互输入，而本文通过类感知提示生成策略实现自动语义解码。具体而言，模型先对增强后的图像嵌入 $E^{\prime}$ 执行全局平均池化，获得全局语义描述向量；再利用两层感知机预测每一类前景目标对应的提示偏移；最终将其与可学习的类别基础嵌入叠加，形成具有类别区分性的类感知提示。设第 $k$ 类提示为 $P_k$，则有：

$$
P_k = P_k^{base} + \mathrm{MLP}\big(\mathrm{GAP}(E^{\prime})\big)_k
$$

式中，$P_k^{base}$ 表示第 $k$ 类的基础可学习嵌入，$\mathrm{GAP}(\cdot)$ 表示全局平均池化，$\mathrm{MLP}(\cdot)$ 表示提示生成器。

在三类分割任务中，前景类别仅包括 `wire` 与 `hole` 两类，因此类感知提示数量固定为 2。提示构造过程并不是对人工交互信号的简单替代，而是将图像的全局语义统计结果映射为稳定的类别查询表示。基础嵌入承担类别先验，提示偏移则反映当前图像中的具体内容，两者叠加后既保留了类别身份，又引入了样本自适应信息。借助这一机制，模型能够在不依赖人工点提示和框提示的情况下自动生成具有类别语义区分性的稀疏提示向量。

### 4.5.2 逐类掩膜解码与语义 logits 重建

提示生成完成后，模型将类别提示依次送入共享的 SAM 掩膜解码器。对于每个前景类别，掩膜解码器接收相同的增强图像特征和稠密提示，类别差异主要由稀疏提示向量体现。模型最终收集各类别的低分辨率输出，并通过 SAM 自带的后处理函数恢复到目标分辨率。

逐类解码方式的优势在于，它避免了对 SAM 掩膜解码器内部结构进行大幅改写，因此能够最大程度保留原始模型的解码能力；同时，不同类别共享同一解码器，使类别差异主要集中编码在提示表示中，从而提升多类分割的稳定性。对于本任务而言，这种处理方式还具有一个额外优点，即线束与孔洞虽然在形态上差异明显，但它们都属于机床电路中的关键前景区域，共享解码器有助于模型在统一的区域建模框架下学习二者之间的差异与联系。

在语义输出阶段，背景类由前景 logits 的负最大值隐式构造，无需额外设计独立的提示和解码分支。该策略与代码中的 `_build_semantic_logits` 实现一致，有利于在保持模型结构简洁的同时恢复完整的三类语义分布。与显式增加一个背景解码分支相比，隐式背景构造将模型容量更集中地分配给 `wire` 与 `hole` 两类关键目标，也避免了背景类在提示空间中占用额外的表示资源。

### 4.5.3 多损失联合优化

为同时提升区域重叠度、边界精度、细线结构连续性和孔洞类别召回率，本文采用 BCE、Dice、Boundary 与 clDice 组成的联合损失函数。在前景类别的 BCE 优化中，考虑到 `hole` 类像素占比较少且更易漏检，本文为该类别设置更高的正样本权重。前景 BCE 损失可表示为：

$$
L_{BCE} = \frac{1}{N}\sum_{k=1}^{C-1} w_k \, \mathrm{BCE}(Z_k, Y_k)
$$

式中，$w_k$ 表示第 $k$ 类的正样本权重，`hole_class_weight` 即对应孔洞类别的正样本增强因子。

在此基础上，本文通过 Dice 损失提升整体区域重叠度，通过 Boundary 损失约束预测边界与真实边界的一致性，并通过 clDice 损失重点保持 `wire` 类的细线连通结构。最终总损失定义为：

$$
L = \lambda_{1} L_{BCE} + \lambda_{2} L_{Dice} + \lambda_{3} L_{Boundary} + \lambda_{4} L_{clDice}
$$

式中，$\lambda_{1}$、$\lambda_{2}$、$\lambda_{3}$ 和 $\lambda_{4}$ 分别表示各项损失权重。在本文实现的默认设置中，`bce_weight=1.0`、`dice_weight=1.0`、`boundary_loss_weight=0.1`、`cldice_weight=0.1`。

这一联合优化策略具有明确的任务针对性。BCE 与 Dice 用于提升整体分割精度，Boundary 用于改善边缘质量，clDice 主要约束线束的拓扑连通性，而 `hole_class_weight` 则专门缓解接口孔洞类别的长尾与漏检问题。相较于仅使用交叉熵或 Dice 的单一损失设计，该联合损失更符合工业机床电路关键区域分割的实际需求。

## 4.6 实验设计与结果分析

本节按照“实验设置 - 对比实验 - 消融实验 - 少样本实验 - 可视化分析 - 失败案例分析”的顺序展开，重点说明实验变量、评价维度以及结果分析的组织方式。

### 4.6.1 数据集、评价指标与实验设置

本章实验基于第3章构建的机床电路关键区域分割数据集开展，训练集、验证集和测试集划分沿用第3章定义，不再重复展开。模型默认使用 `wire_hole` 数据集接口，类别定义为背景、线束与接口孔洞三类。若数据集中未单独设置 `test` 划分，则训练脚本自动退回 `val` 集合作为测试集。

在数据预处理方面，当前实现将输入图像统一缩放至 `1024×1024`，掩膜采用最近邻插值同步缩放。训练阶段可通过 `subset_ratio` 控制训练样本比例，以构建少样本场景。优化器采用 Adam，基础学习率为 `1e-4`，批大小默认为 1。学习率调度器支持 `const` 和 `cosine` 两种设置。主实验采用 `vit_b + medium adapter + compression ratio 8` 作为默认配置，以兼顾性能与显存开销。

评价指标方面，本章重点报告 `IoU`、`Dice`、`Precision`、`Recall`、`F1`、`Boundary F1` 与 `clDice`。其中，`Wire IoU` 用于衡量线束类别的区域提取效果，`Hole IoU` 与 `Hole Recall` 用于衡量接口孔洞类别的检出能力。除精度指标外，模型的工程效率还可结合单图推理时间或 FPS 进行补充度量。

表4-1给出了主实验训练配置。除实验硬件外，其余设置均可由当前实验框架直接支持。

**表4-1 主实验训练配置表**

| 项目 | 设置 |
| --- | --- |
| 实现框架 | PyTorch（基于当前仓库实现） |
| 数据集 | 第3章构建的 `wire_hole` 三类分割数据集 |
| 类别定义 | `background / wire / hole` |
| 输入分辨率 | `1024 × 1024` |
| SAM 主干 | `vit_b`（主实验），可扩展至 `vit_l / vit_h` |
| 适配器规模 | `medium`（主实验），对比 `small / large` |
| 压缩比 | `8`（主实验），对比 `4 / 16 / 32 / 64` |
| 是否残差注入 | `True` |
| 是否简化适配器 | `False`（对照实验设为 `True`） |
| 类感知提示 | `True` |
| 冻结图像编码器 | `True` |
| 冻结提示编码器 | `True` |
| 冻结掩膜解码器 | `False` |
| 优化器 | Adam |
| 学习率 | `1e-4` |
| 调度器 | `const` 或 `cosine` |
| 训练轮次 | `100` |
| 批大小 | `1` |
| 边界损失权重 | `0.1` |
| clDice 权重 | `0.1` |
| 孔洞类别权重 | `2.0` |
| 实验硬件 | 待补 |

上述配置体现了本文在性能、稳定性与计算代价之间的平衡思路。`vit_b` 作为主实验主干，一方面能够继承 SAM 的基础表示能力，另一方面也更适合在有限显存条件下开展大规模对比和消融；`medium` 规模适配器与 `compression ratio 8` 的组合，则兼顾了局部特征增强能力和参数开销控制。冻结图像编码器与提示编码器、保留掩膜解码器可训练的设置，也使模型更适合在工业数据规模相对有限的条件下完成稳定迁移。

### 4.6.2 对比实验设计与分析要点

主对比实验用于验证本文方法相较于原始 SAM 迁移方案和适配器简化方案的有效性。结合当前模型实现，本节设置三类核心对比模型，以便将“是否进行工业特征适配”和“适配器结构是否完整”这两个关键问题区分开来。

- 原始 SAM 迁移版：移除 WireCR 适配器，仅保留与本文一致的数据、损失和训练设置，用于考察未注入工业特征适配时的基础性能。
- SAM + Simple Adapter：启用 `adapter_simple=True`，保留输入投影、多尺度编码和残差注入，但移除显式压缩-扩张瓶颈。
- 完整 WireCR-SAM：采用本文完整的多尺度适配器、类感知提示与联合损失策略。

除与当前实现直接对应的三类模型外，表4-2还预留了 U-Net、DeepLabV3+ 和 SegFormer 等典型语义分割模型的对比位置，以保持比较框架的完整性。对表4-2的分析宜沿着“总体分割质量 - 细线结构保持 - 小目标检出能力”这一逻辑展开。整体 `IoU` 与 `Dice` 反映模型对前景区域的综合刻画能力，`Wire IoU` 与 `clDice` 更能揭示模型对线束连通性和结构完整性的保持水平，而 `Hole IoU` 与 `Hole Recall` 则直接对应接口孔洞类别的定位精度与检出充分性。通过这种分析方式，可以更清楚地说明性能提升究竟来自区域覆盖的改善、边界质量的提高，还是对特定类别结构属性的增强。

**表4-2 主对比实验结果表**

| 模型 | IoU | Dice | Precision | Recall | F1 | Boundary F1 | clDice | Wire IoU | Hole IoU | Hole Recall |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 原始 SAM 迁移版 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 |
| SAM + Simple Adapter | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 |
| WireCR-SAM（本文） | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 |
| U-Net（可选） | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 |
| DeepLabV3+（可选） | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 |
| SegFormer（可选） | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 |

在结果阐释中，可按照“总体精度 - 线束结构质量 - 孔洞召回 - 复杂背景鲁棒性”四个层次展开，从而形成由整体到局部、由区域质量到目标属性的完整分析链条。

如果完整结果表明 WireCR-SAM 在整体指标和类别指标上均优于无适配器基线与简化适配器基线，那么这种优势就不仅意味着“模型更准确”，还说明多尺度特征适配和类感知提示确实改善了工业语义分割中最关键的两类问题：一类是复杂背景下的区域识别稳定性，另一类是细线与小目标的结构保持能力。也正因为如此，主对比实验在本章论证中承担的是“总体有效性证明”的角色。

### 4.6.3 结构消融与损失消融

结构消融主要用于验证适配器内部设计的必要性，本文围绕以下四个维度展开分析。相关实验并不只是比较不同配置下的数值高低，更重要的是说明多尺度编码、瓶颈压缩、上下文门控和类感知提示这些设计是否分别对应了具体的性能收益来源。

- 适配器规模：比较 `small / medium / large` 对性能与计算开销的影响。
- 压缩比：比较 `4 / 8 / 16 / 32 / 64` 对表达能力和参数规模的影响。
- 简化适配器：比较 `adapter_simple=False/True`，验证压缩-扩张瓶颈与上下文门控的作用。
- 类感知提示：比较 `class-aware prompts=True/False`，验证自动类别提示对多类分割能力的影响。

**表4-3 结构消融实验表**

| 实验编号 | adapter size | compression ratio | adapter simple | class-aware prompts | IoU | Dice | Boundary F1 | clDice | Hole Recall |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A1 | small | 8 | False | True | 待补 | 待补 | 待补 | 待补 | 待补 |
| A2 | medium | 8 | False | True | 待补 | 待补 | 待补 | 待补 | 待补 |
| A3 | large | 8 | False | True | 待补 | 待补 | 待补 | 待补 | 待补 |
| A4 | medium | 4 | False | True | 待补 | 待补 | 待补 | 待补 | 待补 |
| A5 | medium | 16 | False | True | 待补 | 待补 | 待补 | 待补 | 待补 |
| A6 | medium | 32 | False | True | 待补 | 待补 | 待补 | 待补 | 待补 |
| A7 | medium | 64 | False | True | 待补 | 待补 | 待补 | 待补 | 待补 |
| A8 | medium | 8 | True | True | 待补 | 待补 | 待补 | 待补 | 待补 |
| A9 | medium | 8 | False | False | 待补 | 待补 | 待补 | 待补 | 待补 |

表4-4进一步给出了损失消融结果，用于分析联合优化策略中各项损失的具体贡献。实验依次比较基础 `BCE + Dice`、加入边界约束、加入 clDice 以及加入孔洞类别权重后的性能变化。这样的设置有助于将区域重叠、边界一致性、细线拓扑保持和类别不平衡抑制等因素逐项拆开讨论，从而避免将所有性能提升简单归因于“联合损失整体有效”这一笼统结论。

**表4-4 损失消融实验表**

| 实验编号 | BCE | Dice | Boundary | clDice | hole weight | IoU | Dice | Boundary F1 | clDice | Hole IoU | Hole Recall |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| L1 | True | True | False | False | 1.0 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 |
| L2 | True | True | True | False | 1.0 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 |
| L3 | True | True | True | True | 1.0 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 |
| L4 | True | True | True | True | 2.0 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 |

结合表4-3和表4-4可以看出，不同结构设计和损失项对应着不同的性能增益来源。适配器规模和压缩比主要影响特征表达能力与参数效率之间的平衡，`adapter_simple` 是否启用则能够反映压缩-扩张瓶颈与上下文门控的实际作用，类感知提示的开闭直接决定类别语义是否能够稳定注入解码过程。与此同时，边界损失主要作用于 `Boundary F1`，clDice 主要影响 `wire` 类的结构连续性，而孔洞类别加权则主要改善 `hole` 类的检出率和召回率。将结构因素与优化因素分开分析，有助于形成更具说服力的实验论证链条。

从论文写作角度看，结构消融与损失消融承担的功能并不相同。前者回答的是“模型为什么这样设计”，关注的是各模块在网络结构中的必要性；后者回答的是“模型为什么这样训练”，强调不同优化目标分别约束了哪些性能维度。只有将这两类实验同时展开，才能使方法创新点与性能提升之间形成较为清晰的因果对应关系。

### 4.6.4 少样本实验设计

少样本实验用于验证本文方法在工业标注资源有限条件下的稳定性。在实现层面，本文通过 `subset_ratio` 参数实现训练集比例采样，从而构造 `0.1/0.25/0.5/1.0` 四种样本量设置。对表4-5的分析重点在于比较不同样本比例下模型性能的变化幅度，以及 `hole` 类在少样本条件下的稳定性。对于工业应用而言，少样本结果的意义并不仅在于说明模型是否“还能工作”，更在于观察性能衰减是否平缓、关键类别是否仍能维持可接受的召回水平，以及模型在训练样本减少时是否仍保持较好的结构一致性。

**表4-5 少样本实验结果表**

| subset ratio | 训练样本数 | IoU | Dice | Wire IoU | Hole IoU | Hole Recall | Boundary F1 | clDice |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0.10 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 |
| 0.25 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 |
| 0.50 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 |
| 1.00 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 | 待补 |

当模型在 25% 或 50% 训练样本条件下仍能保持较高性能时，说明本文方法在标注资源受限的工业场景中仍具备较好的应用潜力。

这一结论对于工业场景尤为重要。与通用数据集相比，机床电路图像的采集、筛选和精细标注成本通常更高，尤其是接口孔洞等小目标类别更容易受到标注尺度和标注一致性的影响。因此，少样本实验不仅是对模型泛化能力的补充验证，也是对其实际部署可行性的直接考察。

### 4.6.5 可视化分析

可视化实验应选取最能体现工业场景难点的典型样本，以便从直观层面补充定量指标难以完全反映的结构差异。样本选择宜覆盖以下几类代表性场景。

- 背景纹理复杂、端子和线槽密集的场景，用于观察模型对背景噪声的抑制能力。
- 线束弯折明显且局部较细的场景，用于观察模型对细线结构连续性的保持能力。
- 孔洞目标尺度较小、数量较少的场景，用于观察模型对小目标的检出能力。

图4-4给出了复杂背景、细线连续和孔洞区域的可视化对比。各组样例依次包括原图、真实标签、原始 SAM 结果、简化适配器结果和 WireCR-SAM 结果，用于比较不同方法在边界对齐、线束断裂抑制和孔洞区域完整提取方面的表现。与表格中的指标相比，可视化结果更适合展示细线是否连续、孔洞轮廓是否完整、背景误检是否集中发生在特定区域等细节，因此在论文论证中具有不可替代的补充作用。

在具体写作时，可视化分析不宜仅停留在“结果更清晰”或“边界更准确”这类笼统描述上，而应结合场景特征指出差异出现的位置与原因。例如，线束弯折处是否出现断裂、孔洞周围是否存在外扩、端子与边框附近是否出现误并入等，都是比单纯视觉印象更有分析价值的观察点。

### 4.6.6 失败案例分析

尽管本文方法能够提升整体分割性能，但在极端复杂场景下仍可能出现以下问题。

- 漏分割：当线束与背景亮度极为接近，或目标被遮挡严重时，模型可能遗漏局部线束区域。
- 边界粘连：当线束与端子、边框或标签区域紧密接触时，模型可能将背景结构误并入目标区域。
- 孔洞误检：当背景中存在圆形金属反光或规则孔状纹理时，模型可能产生误检。

图4-5给出了“漏分割”“边界粘连”“孔洞误检”三类典型失败样本，并在图注中标明主要原因。结合模型结构与训练目标可以作出如下解释：漏分割通常说明小尺度纹理表达仍然不足，边界粘连反映出背景抑制能力仍有提升空间，而孔洞误检则提示类别不平衡和复杂负样本问题尚未完全解决。这些现象表明，模型在困难样本增强、复杂背景抑制和更细粒度结构先验建模方面仍有进一步优化空间。

失败案例分析的意义并不在于否定模型效果，而在于明确当前方法仍然受限于哪些因素。只有将这些误差类型与模型结构、损失设计和数据特征联系起来，后续的改进方向才具有针对性。对于本章提出的方法而言，困难样本增强、复杂背景负样本建模以及更细粒度的结构先验引入，均是值得进一步深入的方向。

## 4.7 本章小结

本章围绕机床电路关键区域分割任务，提出了一种基于多尺度卷积适配与类感知提示的 SAM 分割方法 WireCR-SAM。该方法以原始 SAM 为基础，通过引入 WireCR 多尺度适配器增强工业场景特征表达，并利用可学习的类感知提示实现 `wire` 与 `hole` 两类目标的自动解码，最终通过隐式背景构造完成三类语义分割输出。与此同时，本文设计了由 BCE、Dice、Boundary 和 clDice 组成的联合损失函数，使模型在整体区域精度、边界一致性、细线连通性和孔洞召回方面实现协同优化。

从方法特点看，WireCR-SAM 的优势集中体现在参数高效的工业迁移能力、自动提示驱动的语义解码能力以及面向细线与孔洞结构的针对性优化能力三个层面。模型通过轻量化适配器将工业任务先验注入 SAM 编码特征，避免了全量微调带来的较高训练成本；通过自动生成的类别提示替代人工交互 prompt，使分割流程更契合工业批量检测场景；同时又借助联合损失函数和类别加权机制，强化了对线束连通性、边界质量和孔洞召回性能的约束。

与方法设计相对应，本章进一步建立了由主对比实验、结构消融、损失消融、少样本实验、可视化分析和失败案例分析组成的实验论证框架。该框架不仅服务于模型性能比较，也用于解释不同模块和不同优化目标分别带来的性能来源，从而使本章关于 WireCR-SAM 的方法论证、实验分析和工程适用性讨论形成较为完整的闭环。
