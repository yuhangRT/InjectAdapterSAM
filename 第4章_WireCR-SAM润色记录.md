# 第4章 WireCR-SAM 逐节润色记录

## 4.1 问题描述与总体思路

### 修改说明（按准则归类）

- `句式多样化`：将原有连续枚举式表达调整为“任务性质 - 场景难点 - 下游需求”的推进结构，避免整段均以同一模板展开。
- `杜绝模糊表达`：将“难度较大”“要求较高”等表述具体化为“局部结构表征能力”“细线连通性”“孔洞召回能力”等可分析对象。
- `动词强化`：用“建模”“提取”“增强”“重建”等动词替换“进行”“实现”等较弱表达，增强方法链路的清晰度。

### 需要你确认的事实核查清单

- “分割结果将直接服务于后续的字符裁剪与检测分析”是否与第1章和第3章的技术路线表述完全一致。
- 三类类别名“背景、线束、接口孔洞”是否与第3章数据集定义逐字统一。

## 4.2 原始 SAM 模型分析与适配需求

### 修改说明（按准则归类）

- `句式多样化`：将“首先/其次/再次/最后”的局限分析保留为清晰层次，但在段内引入定义句、解释句和结论句，减轻平铺直叙感。
- `避免陈词滥调`：删除不必要的铺垫性描述，直接围绕编码域差异、prompt 机制、小目标与语义类别解码四个限制展开。
- `动词强化`：将“有较强能力”类表述改为“具备”“保留”“约束”“建立映射”等更精确的学术动词。
- `合理使用转折`：减少机械性转折，将对 SAM 的评价调整为“优势 - 局限 - 迁移要求”的自然过渡。

### 需要你确认的事实核查清单

- “SAM 图像编码输出通道数固定为 256”是否在你全文其他章节中也沿用这一实现口径。
- “不引入独立后处理网络”的表述是否需要在第1章创新点或第4章引言中提前呼应。

## 4.3 WireCR-SAM 总体框架

### 修改说明（按准则归类）

- `清晰优先`：将总体框架改写为“新增模块说明 + 流程列表 + 隐式背景策略 + 工程优势”四层结构，减少技术描述跳跃。
- `一致性`：统一使用“类别提示”“逐类解码”“隐式背景构造”“语义 logits”等术语，避免与前后章节出现近义替换。
- `动词强化`：将“得到”“形成”等泛化动词替换为“叠加”“调用”“恢复”“拼接”等更贴近实现流程的表达。

### 需要你确认的事实核查清单

- 背景类由前景 logits 的负最大值构造，这一机制若需在论文中写成公式推导，是否还要补充符号说明。
- “主任务 `wire_hole` 与辅助任务 `coco` 共用同一训练入口”是否需要在正文中强调为工程实现特性，而非方法本体贡献。

## 4.4 WireCR 多尺度适配器设计

### 修改说明（按准则归类）

- `句式多样化`：把三个子节分别稳定为“模块目标 - 公式定义 - 结构解释 - 实验意义”的组织方式，减少同质化说明。
- `杜绝模糊表达`：将“更适合”“更强”一类判断补足为“更有利于建模方向性与上下文关联”“系统分析参数规模与性能关系”等更具体说法。
- `动词强化`：强化“投影”“捕获”“调制”“回映射”“注入”等过程动词，使模块功能边界更明确。
- `一致性`：统一 `WireCR adapter`、`compression_ratio`、`adapter_simple`、`CRBlock` 等核心结构术语。

### 需要你确认的事实核查清单

- “双路径结构来源于 CRNet”是否需要在最终论文中补相应文献引用。
- `adapter_simple` 是否计划在实验章节中保留原参数名，还是改写为中文描述后仅在括号中标注。

## 4.5 类感知提示生成与联合优化策略

### 修改说明（按准则归类）

- `清晰优先`：将本节拆解为“提示构造 - 解码方式 - 损失设计”三个稳定子问题，强化因果链条。
- `合理使用转折`：减少“而本文”反复出现的句式，改为直接陈述机制和作用。
- `动词强化`：用“叠加”“接收”“恢复”“约束”“缓解”等动词替代偏弱表达。
- `一致性`：统一 `Boundary`、`clDice`、`hole_class_weight`、`_build_semantic_logits` 对应的中文解释。

### 需要你确认的事实核查清单

- 损失权重的默认值是否会与最终实验设置保持一致，若后续修改需同步更新本节与表4-1。
- `hole_class_weight` 在论文中是否需要额外说明具体取值依据，例如经验设定或验证集选择。

## 4.6.1 数据集、评价指标与实验设置

### 修改说明（按准则归类）

- `避免陈词滥调`：将“建议采用”“后续可补充”类口吻收束为实验章节的客观表述，保留必要占位但避免计划书风格。
- `清晰优先`：按“数据来源 - 预处理 - 训练配置 - 指标体系 - 配置表”顺序重组叙述，便于直接落入论文实验节。
- `一致性`：统一指标名称为 `IoU`、`Dice`、`Boundary F1`、`clDice`、`Wire IoU`、`Hole Recall`。

### 需要你确认的事实核查清单

- 实验硬件是否后续单列 GPU 型号、显存和 CUDA 版本。
- 训练轮次、批大小和学习率是否已经固定为最终论文版本。

## 4.6.2 对比实验设计与分析要点

### 修改说明（按准则归类）

- `清晰优先`：将本节改写为“实验目的 - 基线组成 - 关键分析问题 - 表格模板”的标准结构。
- `杜绝模糊表达`：将“有效性”具体化为整体精度、线束结构质量和孔洞召回三类比较目标。
- `合理使用转折`：弱化口语化说明，改为论文中更自然的分析导向表达。

### 需要你确认的事实核查清单

- 原始 SAM 迁移版是否已经在你的实验设计中明确可复现。
- U-Net、DeepLabV3+、SegFormer 是否真的会纳入最终论文；若不会，应提前从表中删去。

## 4.6.3 结构消融与损失消融

### 修改说明（按准则归类）

- `句式多样化`：将结构消融和损失消融分别写成“维度说明 + 表格模板 + 结果解释重点”的模式，增强条理性。
- `动词强化`：使用“验证”“比较”“约束”“提升”等更具学术功能的动词。
- `一致性`：统一 `adapter_simple`、`class-aware prompts`、`Boundary F1`、`Hole Recall` 等实验变量与指标写法。

### 需要你确认的事实核查清单

- 消融实验中的 A1-A9、L1-L4 编号是否与你最终实验记录表一致。
- `Hole Recall` 是否是你最终要重点保留的孔洞类别指标，还是还需补充 `Hole Dice`。

## 4.6.4 少样本实验设计

### 修改说明（按准则归类）

- `杜绝模糊表达`：将“少样本稳定性”具体化为“性能衰减速度”和“孔洞类别稳定性”两个观察维度。
- `动词强化`：将“支持”“构造”“关注”等动作明确对应到代码参数和实验分析。

### 需要你确认的事实核查清单

- `subset_ratio=0.1/0.25/0.5/1.0` 是否与你最终实验采样方案完全一致。
- 表4-5 中“训练样本数”是否后续会填写具体样本量，而不是仅保留比例。

## 4.6.5 可视化分析

### 修改说明（按准则归类）

- `避免陈词滥调`：删除“只描述结果更好”类提醒式写法，改为直接规定应观察的可视化现象。
- `清晰优先`：将可视化对象拆解为复杂背景、细线连续性和小目标检出三类典型场景。

### 需要你确认的事实核查清单

- 图4-4 最终是否真的包含“原始 SAM、简化适配器、WireCR-SAM”三种对比结果。
- 可视化样例是否需要来自测试集而非验证集，以避免正文与实验口径不一致。

## 4.6.6 失败案例分析

### 修改说明（按准则归类）

- `清晰优先`：将失败模式归纳为漏分割、边界粘连和孔洞误检三类，避免原因与现象混写。
- `动词强化`：将“可能说明”“仍需处理”等表达与模型缺陷、样本分布问题对应起来，使分析更具解释性。

### 需要你确认的事实核查清单

- 图4-5 中三类失败样本是否都已经准备好。
- “增强困难样本、补充负样本、引入结构先验”是否只作为展望，还是后续章节还会继续展开。

## 4.6 实验节统一收束

### 修改说明（按准则归类）

- `一致性`：统一实验节的叙述口吻，将“建议”“可选”“后续补充”控制在必要范围内，避免实验章节呈现明显计划书风格。
- `句式多样化`：统一各子节开头方式，避免连续多节都以“本节/本章实验”同模板起句。
- `引用优化`：统一图4-4、图4-5、表4-1至表4-5的引导语，使其适合直接转为论文正文。

### 需要你确认的事实核查清单

- 表4-1至表4-5是否还需要统一英文变量大小写和命名方式。
- 实验节是否需要单独增加“实现环境”或“评价协议”小段以匹配学院论文模板。

## 4.7 本章小结

### 修改说明（按准则归类）

- `清晰优先`：将小结收束为“方法概括 - 三点优势 - 后续验证方向”的稳定结构。
- `动词强化`：以“注入”“替代”“构建”“验证”等动词增强总结力度，避免口号式结论。
- `一致性`：确保本章小结中的核心术语与前文方法节保持一致。

### 需要你确认的事实核查清单

- 小结中“三点优势”是否与第1章创新点一一对应，若不对应建议后续统一。
- “后续实验将围绕……”这一表述若在终稿中实验已补全，需改为完成时表述。

## 全章一致性总结

### 修改说明（按准则归类）

- `一致性`：全章统一使用“原始 SAM”“WireCR-SAM”“类感知提示”“隐式背景构造”“Boundary F1”“clDice”等核心术语。
- `句式多样化`：方法节、实验节和小结的句式已经分层处理，避免整章出现连续的提纲式枚举。
- `避免陈词滥调`：清理了工程文档口吻和计划书口吻，使主稿更接近硕士论文正文表达。

### 需要你确认的事实核查清单

- 公式符号 $E$、$E^{\prime}$、$P_k$、$Z_k$、$S$、$L$ 是否与全文其他章节的记号体系冲突。
- 图4-1至图4-5、表4-1至表4-5 是否已经按学院模板确定编号和标题格式。
- 主稿中所有“待补”位置后续是否都有明确的数据、图像或硬件信息来源。
- 如需提交终稿，应再做一次全章层面的引用检查，补充 CRNet、SAM、clDice 等相关文献。
