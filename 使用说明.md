# InjectAdapterSAM 使用说明

## 1. 项目定位

当前仓库的主模型可以表述为 `WireCR-SAM`：

- 以 SAM 作为图像编码与掩码解码骨架
- 以 CRNet 风格模块作为中间特征适配器
- 以自动类感知 prompts 代替旧版手工 prompt 流程

当前任务分为两类：

1. `wire_hole`  
   主任务，面向工业机床电路线束与接口孔洞的三类语义分割，也是论文主线。

2. `coco`  
   辅任务，面向 COCO 的二类前景分割，只用于辅助预训练、迁移或烟测，不建议作为论文主实验。

---

## 2. 当前代码接口和旧版差异

当前代码已经不再使用下面这些旧接口：

- `--prompt-strategy`
- `--num-prompts`
- 人工点/框提示作为训练入口

当前实际生效的是：

- `--dataset wire_hole|coco`
- `--num-classes`
- `--class-aware-prompts`
- `--boundary-loss-weight`
- `--cldice-weight`
- `--hole-class-weight`
- `--subset-ratio`

如果你看到仓库里还有旧版 COCO/prompt 文档或脚本，请以本文件和 `README.md` 为准。

---

## 3. 环境安装

### 3.1 基础安装

```bash
git clone git@github.com:yuhangRT/InjectAdapterSAM.git
cd InjectAdapterSAM
git submodule update --init --recursive
pip install -r requirements_sam.txt
```

### 3.2 推荐权重

显存有限时优先用 `vit_b`：

```bash
mkdir -p checkpoints
wget https://dl.fbaipublicfiles.com/segment_anything/sam_vit_b_01ec64.pth -P checkpoints/
```

更大模型：

```bash
wget https://dl.fbaipublicfiles.com/segment_anything/sam_vit_l_0b3195.pth -P checkpoints/
wget https://dl.fbaipublicfiles.com/segment_anything/sam_vit_h_4b8939.pth -P checkpoints/
```

### 3.3 验证安装

```bash
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available())"
python -c "from segment_anything import sam_model_registry; print(sorted(sam_model_registry.keys()))"
python -m compileall main.py main_sam.py models dataset utils
```

---

## 4. 数据准备

## 4.1 `wire_hole` 主任务

标签语义：

- `0`: background
- `1`: wire
- `2`: interface-hole

支持的目录结构一：

```text
<data-root>/
├── images/
│   ├── train/
│   ├── val/
│   └── test/
└── masks/
    ├── train/
    ├── val/
    └── test/
```

支持的目录结构二：

```text
<data-root>/
├── train/
│   ├── images/
│   └── masks/
├── val/
│   ├── images/
│   └── masks/
└── test/
    ├── images/
    └── masks/
```

说明：

- `labels/` 可以替代 `masks/`
- mask 可以是单通道整数标签图，也可以是离散颜色图，但最终必须能映射到 3 类

## 4.2 `coco` 辅助任务

当前实现不是 COCO 原始多类实例分割，而是把一张图中所有实例合并为统一前景。

因此 COCO 模式必须设置：

```bash
--dataset coco --num-classes 2
```

支持目录结构一：

```text
<data-root>/
├── train2017/
├── val2017/
└── annotations/
    ├── instances_train2017.json
    └── instances_val2017.json
```

支持目录结构二：

```text
<data-root>/
├── images/
│   ├── train2017/
│   └── val2017/
└── annotations/
    ├── instances_train2017.json
    └── instances_val2017.json
```

说明：

- 训练时把所有实例 mask 合并成一个前景 mask
- 如果 `test` 标注不存在，主程序会自动退回 `val` 作为测试集

---

## 5. 训练

## 5.1 `wire_hole` 主任务最小可行配置

```bash
python main_sam.py \
  --mode sam \
  --data-dir /path/to/wire_hole \
  --dataset wire_hole \
  --num-classes 3 \
  --sam-model-type vit_b \
  --sam-checkpoint ./checkpoints/sam_vit_b_01ec64.pth \
  --batch-size 1 \
  --workers 0 \
  --epochs 1 \
  --adapter-size small \
  --compression-ratio 16 \
  --class-aware-prompts \
  --boundary-loss-weight 0.1 \
  --cldice-weight 0.1 \
  --hole-class-weight 2.0 \
  --subset-ratio 0.1
```

## 5.2 `wire_hole` 正式训练建议

```bash
python main_sam.py \
  --mode sam \
  --data-dir /path/to/wire_hole \
  --dataset wire_hole \
  --num-classes 3 \
  --sam-model-type vit_b \
  --sam-checkpoint ./checkpoints/sam_vit_b_01ec64.pth \
  --batch-size 1 \
  --workers 0 \
  --epochs 100 \
  --adapter-size medium \
  --compression-ratio 8 \
  --class-aware-prompts \
  --boundary-loss-weight 0.1 \
  --cldice-weight 0.1 \
  --hole-class-weight 2.0 \
  --subset-ratio 1.0
```

如果显存更大，可逐步尝试：

- `vit_l`
- `adapter-size large`
- 更小的压缩比，如 `8 -> 4`

## 5.3 `coco` 辅助训练

```bash
python main_sam.py \
  --mode sam \
  --data-dir /path/to/coco \
  --dataset coco \
  --num-classes 2 \
  --sam-model-type vit_b \
  --sam-checkpoint ./checkpoints/sam_vit_b_01ec64.pth \
  --batch-size 1 \
  --workers 0 \
  --epochs 10 \
  --adapter-size small \
  --compression-ratio 16 \
  --class-aware-prompts \
  --boundary-loss-weight 0.1 \
  --cldice-weight 0.0 \
  --subset-ratio 0.1
```

适用场景：

- 代码链路验证
- 辅助预训练
- 泛化补充实验

不建议用途：

- 替代工业主实验
- 直接作为毕业论文核心结论

---

## 6. 评估

### 6.1 `wire_hole` 评估

```bash
python main_sam.py \
  --mode sam \
  --evaluate \
  --data-dir /path/to/wire_hole \
  --dataset wire_hole \
  --num-classes 3 \
  --sam-model-type vit_b \
  --sam-checkpoint ./checkpoints/sam_vit_b_01ec64.pth \
  --pretrained ./checkpoints/wirecrsam_wire_hole_medium_cr8/best_iou.pth \
  --batch-size 1 \
  --workers 0 \
  --adapter-size medium \
  --compression-ratio 8
```

说明：

- 每次训练或评估结束后，程序都会自动写出 `experiment_summary.json`
- 如需指定实验目录，可加 `--run-name` 与 `--save-dir`
- 如需固定结构化结果文件路径，可加 `--results-json`

### 6.2 `coco` 评估

```bash
python main_sam.py \
  --mode sam \
  --evaluate \
  --data-dir /path/to/coco \
  --dataset coco \
  --num-classes 2 \
  --sam-model-type vit_b \
  --sam-checkpoint ./checkpoints/sam_vit_b_01ec64.pth \
  --pretrained ./checkpoints/wirecrsam_coco_small_cr16/best_iou.pth \
  --batch-size 1 \
  --workers 0 \
  --adapter-size small \
  --compression-ratio 16
```

---

## 7. 论文实验套件与 CSV 导出

### 7.1 运行表 4-2 到表 4-5 对应的实验

推荐使用：

```bash
python scripts/run_thesis_suite.py \
  --table 4-2 \
  --output-root ./thesis_runs \
  --data-dir /path/to/wire_hole \
  --dataset wire_hole \
  --num-classes 3 \
  --sam-model-type vit_b \
  --sam-checkpoint ./checkpoints/sam_vit_b_01ec64.pth \
  --batch-size 1 \
  --workers 0 \
  --epochs 100 \
  --adapter-size medium \
  --compression-ratio 8
```

支持：

- `--table 4-2`：主对比实验
- `--table 4-3`：结构消融实验
- `--table 4-4`：损失消融实验
- `--table 4-5`：少样本实验
- `--table all`：依次执行全部论文实验套件

如需只查看实际命令而不执行，可加 `--dry-run`。

### 7.2 直接导出表格 CSV

导出表 4-1：

```bash
python scripts/export_thesis_tables.py \
  table4_1 \
  --reference ./thesis_runs/table4_2/table4_2_wirecr_sam \
  --output ./thesis_tables/table4_1.csv
```

导出表 4-2：

```bash
python scripts/export_thesis_tables.py \
  table4_2 \
  --manifest ./thesis_runs/table4_2/manifest_table4_2.json \
  --output ./thesis_tables/table4_2.csv
```

导出表 4-3：

```bash
python scripts/export_thesis_tables.py \
  table4_3 \
  --manifest ./thesis_runs/table4_3/manifest_table4_3.json \
  --output ./thesis_tables/table4_3.csv
```

导出表 4-4：

```bash
python scripts/export_thesis_tables.py \
  table4_4 \
  --manifest ./thesis_runs/table4_4/manifest_table4_4.json \
  --output ./thesis_tables/table4_4.csv
```

导出表 4-5：

```bash
python scripts/export_thesis_tables.py \
  table4_5 \
  --manifest ./thesis_runs/table4_5/manifest_table4_5.json \
  --output ./thesis_tables/table4_5.csv
```

---

## 8. 指标解释

程序当前会输出：

- `IoU`
- `Dice`
- `Precision`
- `Recall`
- `F1`
- `Boundary F1`
- `clDice`

在 `wire_hole` 下，还会额外输出：

- `Wire IoU`
- `Hole IoU`
- `Hole Recall`

在 `coco` 下，会额外输出：

- `Foreground IoU`

---

## 9. 参数说明

| 参数 | 说明 |
|------|------|
| `--dataset` | `wire_hole` 或 `coco` |
| `--num-classes` | `wire_hole=3`, `coco=2` |
| `--sam-model-type` | `vit_b`, `vit_l`, `vit_h` |
| `--adapter-size` | `small`, `medium`, `large` |
| `--compression-ratio` | `4`, `8`, `16`, `32`, `64` |
| `--class-aware-prompts` | 启用自动类感知 prompts |
| `--freeze-encoder` | 是否冻结 SAM 图像编码器 |
| `--freeze-decoder` | 是否冻结 SAM 掩码解码器 |
| `--freeze-prompt-encoder` | 是否冻结 prompt encoder |
| `--subset-ratio` | 训练子集比例，支持少样本实验 |
| `--boundary-loss-weight` | 边界损失权重 |
| `--cldice-weight` | 细线结构一致性权重 |
| `--hole-class-weight` | `wire_hole` 中 hole 类正样本权重 |
| `--disable-adapter` | 关闭 WireCR adapter，作为无适配器对比基线 |
| `--run-name` | 指定实验名 |
| `--save-dir` | 指定实验输出根目录 |
| `--results-json` | 指定结构化实验结果 JSON 路径 |

---

## 10. 常见问题

### Q1. `coco` 可以直接作为当前论文主实验吗？

不建议。当前论文主线应使用 `wire_hole`。`coco` 更适合作为辅助迁移或烟测数据。

### Q2. 为什么 `--dataset coco` 时还要设置 `--num-classes 2`？

因为当前实现把所有实例合并为统一前景，并不是做 COCO 原始多类实例分割。

### Q3. 为什么旧文档里还有 `prompt-strategy`、`num-prompts`？

那是旧版交互提示训练接口，当前主线已经改成自动类感知 prompts，不再使用。

### Q4. `scripts/grid_search.sh` 能直接跑吗？

当前不能直接作为主线实验脚本使用。它仍然保留了旧版 COCO/prompt 参数，需要你后续按当前接口再改。

### Q5. 显存不够怎么办？

先按这个顺序降：

1. `vit_h -> vit_l -> vit_b`
2. `medium -> small`
3. `compression-ratio 8 -> 16`
4. `batch-size 1`

---

## 10. 建议的论文实验策略

主实验：

- `wire_hole`
- `num-classes 3`
- 比较不同 adapter size / compression ratio
- 做少样本 `subset-ratio`

辅助实验：

- `coco`
- `num-classes 2`
- 用于辅助预训练或泛化补充

如果你的目标是硕士论文主线，结论应围绕：

- 工业细线结构分割
- 小样本稳定性
- WireCR adapter 对 SAM 的适配效果
